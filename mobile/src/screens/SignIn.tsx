import { Center, StatusBar, Text } from 'native-base'
import React from 'react'

export function SignIn() {
    return (
        <Center flex={1} bg='black' alignItems={'center'} justifyContent={'center'}>
            <Text color={'gray.200'} fontSize={24}>
                React Native
            </Text>

        </Center>

    )
}